apiVersion: 1

dashboards:
  - name: "spring-boot"
    orgId: 1
    folder: "Platform"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
